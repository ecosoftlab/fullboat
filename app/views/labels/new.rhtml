<h1>Create a New Label</h1>

<% remote_form_for(:label, :url => labels_path) do |f| %>
	<%= render(:partial => "labels/form", :locals => {:f => f})%>
  <%= submit_tag "Create" %>
<% end %>

<%= link_to 'Back', labels_path %>