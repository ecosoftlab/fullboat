<h1>Programming</h1>

<h2>Now Playing on WRCT</h2>
<% if @schedule %>
  <%= render(:partial => 'programming/schedules/summary', :object => @schedule, 
             :locals => {:start_hour => Time.now.hour, :end_hour => (Time.now.hour + 4) % 24}) %>
<% else %>
  <p>No current schedule was found to be displayed...</p>
  <p>That sucks.</p>
<% end %>