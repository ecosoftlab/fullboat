<h1>Create a New Schedule</h1>

<% remote_form_for(:schedule, :url => schedules_path) do |f| %>
  <%= render(:partial => "schedules/form", :locals => {:f => f})%>
  <%= submit_tag "Create" %>
<% end %>

<%= link_to 'Back', schedules_path %>